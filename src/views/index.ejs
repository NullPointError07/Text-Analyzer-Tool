<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800">
    <%- include('partials/navbar') %>
        <div class="container mx-auto mt-10 text-center">
            <h1 class="text-3xl font-bold mb-4">Welcome to Text Analyzer</h1>

            <% if (isAuthenticated) { %>
                <!-- If the user is logged in, show logout button -->
                <a href="/logout"
                    class="inline-block bg-red-500 text-white font-medium px-6 py-3 rounded hover:bg-red-600 transition">
                    Log out
                </a>
                <% } else { %>
                    <!-- If the user is not logged in, show login button -->
                    <a href="/auth/google"
                        class="inline-block bg-blue-500 text-white font-medium px-6 py-3 rounded hover:bg-blue-600 transition">
                        Log in to continue
                    </a>
                    <% } %>
        </div>
</body>

</html>