<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-100 text-gray-800 flex justify-center items-center min-h-screen">
    <!-- Main Container -->
    <div class="bg-white shadow-lg rounded-lg w-full max-w-4xl p-6">
        <!-- Navbar inclusion -->
        <%- include('partials/navbar') %>

            <h1 class="text-3xl font-bold text-center mb-6 mt-4">Welcome to Text Analyzer</h1>

            <div class="flex justify-center space-x-4">
                <% if (isAuthenticated) { %>
                    <!-- If the user is logged in, show logout button -->
                    <a href="/auth/logout"
                        class="bg-red-500 text-white py-3 px-6 rounded-lg hover:bg-red-600 transition-all focus:outline-none focus:ring-2 focus:ring-red-500">
                        Log out
                    </a>
                    <% } else { %>
                        <!-- If the user is not logged in, show login button -->
                        <a href="/auth/google"
                            class="bg-blue-600 text-white py-3 px-6 rounded-lg hover:bg-blue-700 transition-all focus:outline-none focus:ring-2 focus:ring-blue-500">
                            Log in to continue
                        </a>
                        <% } %>
            </div>
    </div>
</body>

</html>