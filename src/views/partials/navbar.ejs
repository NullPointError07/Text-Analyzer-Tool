<nav class="bg-gray-800 text-white px-6 py-4 flex justify-between items-center rounded-lg shadow-md">
    <a href="/" class="text-xl font-bold">Text Analyzer</a>
    <div>
        <% if (isAuthenticated) { %>
            <!-- If the user is authenticated, show logout button -->
            <a href="/auth/logout"
                class="px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-600 transition-all focus:outline-none focus:ring-2 focus:ring-red-500">
                Logout
            </a>
            <% } else { %>
                <!-- If the user is not authenticated, show login button -->
                <a href="/auth/google"
                    class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-all focus:outline-none focus:ring-2 focus:ring-blue-500">
                    Login
                </a>
                <% } %>
    </div>
</nav>